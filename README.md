# Файлы для итогового задания

В директории `tests` находятся тесты для проверки API, которое должно быть реализовано в веб-сервере.

Директория `web` содержит файлы фронтенда.



Для логина в планировщик переходите по адресу: "http://localhost:7540/login.html" и вводите пароль "1234", он записан в .ENV

Задания со свездочкой не делались. 

Собрать образ Docker: docker build -tag practicum_final:v1 .
Запустить контейнер Docker: docker-compose up -d